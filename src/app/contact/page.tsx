"use client";
import React, { useState } from "react";
import ContactForm from "./contactForm";
type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <ContactForm />
    </div>
  );
};

export default page;
